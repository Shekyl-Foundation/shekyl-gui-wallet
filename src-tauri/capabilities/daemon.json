{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "daemon-sidecar",
  "description": "Allow spawning and managing the bundled shekyld daemon",
  "windows": ["main"],
  "permissions": [
    "shell:allow-spawn",
    "shell:allow-kill",
    "shell:allow-stdin-write"
  ]
}
